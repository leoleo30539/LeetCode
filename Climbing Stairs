class Solution(object):
    def climbStairs(self, n):
        """
        :type n: int
        :rtype: int
        """
        if n == 1:
            return 1
        x = [1, 2]
        for i in range(n-2):
            x.append(x[-1]+x[-2])
        return x[-1]
